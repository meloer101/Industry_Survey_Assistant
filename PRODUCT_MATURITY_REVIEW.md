# 产品成熟度审视报告

> 审视日期：2026-06-15  
> 视角：资深 AI 产品经理  
> 评分维度：6 项，满分 5 分

---

## 概述

本报告基于对全部源码的深度审查，从**成熟产品**、**差异化产品**、**生产级服务**三个目标出发，评估当前项目在六个核心维度上的现状与差距。

---

## 六维评分总览

| 维度 | 得分 | 最大短板 |
|------|------|---------|
| 核心 AI 流水线 | ⭐⭐⭐⭐☆ 4/5 | 无输出质量监控、无缓存层 |
| 工程质量 | ⭐⭐⭐½☆ 3.5/5 | 无 E2E 测试、无负载测试 |
| 安全与认证 | ⭐⭐☆☆☆ 2/5 | 无用户认证、无 API 限流、无审计日志 |
| UX 与差异化 | ⭐⭐½☆☆ 2.5/5 | 无移动端、无进度指示、无报告交互 |
| 运维与可观测性 | ⭐⭐½☆☆ 2.5/5 | 无告警规则、无成本追踪 |
| 增长与留存 | ⭐½☆☆☆ 1.5/5 | 无用户体系、无分享、无反馈 |

---

## 一、核心 AI 流水线（4/5）— 最强的部分

### 做得好的

- 8 Agent 层次化流水线（Plan → Research → Evaluate → Loop → Analyze → Report）架构清晰
- 引用溯源系统完整（`grounding_metadata` → `<cite>` → markdown link），这是大多数竞品做不到的
- 自检循环（LoopAgent + EscalationChecker）实现了研究质量的自动收敛
- 分析路由器（macro / fundamental / risk）根据主题智能决策调用顺序
- 速率限制、超时保护、pipeline 容错（`PipelineGuard`）都已到位

### 离生产级的差距

- **无模型输出质量监控**：研究报告生成后没有自动评分机制，无法量化"这篇报告质量如何"
- **无 A/B 测试框架**：换模型、换 prompt、换搜索策略后，无法对比哪个版本更好
- **单一搜索源**：只有 Tavily，如果它挂了或结果质量下降，整个研究流水线的数据来源就断了
- **无缓存层**：同一话题短时间内被多次研究，每次都重新搜索、重新调用 LLM，成本和延迟没有优化

---

## 二、工程质量（3.5/5）— 基础扎实但不够完整

### 做得好的

- CI 三管齐下（pytest + ruff + tsc + npm test）
- 后端 1048 行测试 + 前端 287 行测试，覆盖了 callbacks、tools、persistence
- Dockerfile + docker-compose 容器化就绪
- TypeScript strict 模式开启
- 结构化日志（JSON/text 双格式 + request_id 链路追踪）

### 差距

- **无端到端测试**：没有 Playwright/Cypress 验证"用户输入主题 → 看到报告"的完整流程
- **无负载测试**：不知道系统在 10 个并发用户时会怎样
- **前端 `App.tsx` 仍有 469 行**：核心状态管理全在一个组件里，没有用 Zustand/Context 抽离，维护成本随功能增加会急剧上升
- **无数据库迁移方案**：SQLite 直连，schema 变更全靠 ADK 内部处理，生产环境切 PostgreSQL 时没有迁移工具
- **缺乏 integration 层测试**：有单元测试但没有"启动真实服务 → 发 HTTP 请求 → 验证 SSE 响应"的集成测试

---

## 三、安全与认证（2/5）— 最大的短板

### 现状（已有）

- API Key 认证存在（header-only，明确排除了 cookie CSRF）
- CORS 白名单配置正确
- `.env` 文件已正确排除在 git 之外

### 严重缺失

- **无用户认证系统**：`userId` 存在 localStorage，任何人改一下就能访问别人的研究历史。这对金融产品来说不可接受
- **无输入清洗/注入防护**：用户输入的研究主题直接拼入 LLM prompt，无 prompt injection 防护
- **无请求频率限制（API 层）**：LLM 调用有 RPM 限制，但 HTTP API 层面没有 rate limiting，一个恶意用户可以无限触发昂贵的研究流水线
- **无审计日志**：谁在什么时间研究了什么主题、消耗了多少 token，完全没有记录
- **无内容安全检查**：用户可能输入敏感/违规内容，输出也没有过滤机制
- **API Key 是单一全局密钥**：没有 per-user 的 API key 或 token 机制

---

## 四、UX 与差异化（2.5/5）— 有亮点但缺打磨

### 亮点（也是差异化来源）

- Landing Page 精致，8 Agent 可视化、HITL 流程图、示例话题一键启动
- 实时 SSE 流式展示研究过程（用户能看到 AI 在搜什么、分析什么），体验远优于"等 3 分钟出报告"
- 报告导出（Markdown）已实现
- 研究历史面板带分页
- 引用溯源让报告可信度远超 ChatGPT 等通用工具

### 缺失的体验层

- **无移动端适配**：从代码看没有响应式断点处理，手机上大概率不可用
- **无研究进度指示器**：SSE 流式只展示"当前 Agent 在做什么"，但用户不知道"大概还要多久"、"进度到了百分之几"
- **无报告交互功能**：报告生成后是纯静态 markdown，没有图表、没有可折叠章节、没有"追问 AI 关于这段分析"的能力
- **无 Onboarding 引导**：新用户第一次进入研究界面后没有引导
- **中英混杂**：UI 文案主要中文但部分地方混英文，没有 i18n 框架
- **无深色模式**：金融用户长时间使用，这是基本预期
- **Phase 2 HITL 未实现**：研究完成后、分析前的二次确认节点还没做，用户无法在分析阶段介入

---

## 五、运维与可观测性（2.5/5）— 框架有了，但不够用

### 已有

- `/health` 端点 + Docker HEALTHCHECK
- `/metrics` 暴露 Prometheus 格式的 pipeline 指标（运行数、失败数、耗时、引用源数）
- OpenTelemetry tracing 集成（有 span）
- Session TTL 自动清理
- 结构化 JSON 日志

### 缺失

- **无告警规则**：metrics 暴露了但没有对接 Alertmanager 或任何告警系统
- **无成本追踪**：每次研究消耗多少 LLM token / 搜索 API 调用，完全没有计量
- **无 Grafana/Dashboard 模板**：有了 metrics 端点但没有可视化
- **无错误率 SLO**：什么算"系统正常"没有定义
- **无灰度发布能力**：一部署就是全量，没有金丝雀或蓝绿策略
- **SQLite 在生产环境的局限**：单机锁、无法水平扩展，切 PostgreSQL 的路径没有文档化

---

## 六、增长与留存（1.5/5）— 几乎空白

这是离"成熟产品"距离最远的维度。

- **无用户注册/登录**：没有用户体系就无法谈留存
- **无用户行为追踪**：不知道用户看了报告多久、是否导出、是否回来
- **无分享功能**：研究报告无法分享给他人（链接分享、PDF 分享）
- **无通知/推送**：研究完成后（尤其长时间研究）没有通知机制
- **无定价/计量模型**：免费无限用 = 无法商业化
- **无反馈收集**：报告好不好用、准不准确，没有用户反馈入口
- **无 SEO**：Landing Page 做了但没有 meta tags、结构化数据、社交分享卡片

---

## 三个目标维度的差距总结

### 成熟产品

核心缺口是**用户体系**。没有注册/登录/权限体系，一切基于 userId 的隔离和个性化都是沙上建塔。其次是移动端适配和 Onboarding，这是用户从"第一次用"变成"习惯用"的关键路径。

### 有差异度的产品

引用溯源 + 实时可视化 + HITL 已经是真实的差异化，竞品（ChatGPT、Perplexity）都没有做到这种程度的研究过程透明化。但护城河需要加深：**报告内追问**（生成后继续对话）、**图表可视化**（把数据变成图）、**多报告对比**——这些功能能把"工具"变成"产品"。

### 生产级服务

安全认证 → API 限流 → 成本计量 → 告警规则 → 负载测试，是一个必须串行完成的链条。缺了前面任何一环，部署到生产都是高风险的。

---

## 优先级建议

如果只能做三件事：

**1. 用户认证系统**（OAuth2 / JWT）
- 解决安全、增长、留存的共同基础设施问题
- 没有它，其他所有功能都建立在不安全的 userId 假设上

**2. API 层 rate limiting + 成本计量**
- 没有这个，一个用户就能烧光 LLM 预算
- 成本计量是定价模型的前提

**3. 报告交互能力**（追问 + 数据图表）
- 这是把"一次性工具"变成"持续使用的产品"的关键
- 竞品难以快速复制，差异化护城河

---

## 附：技术债快照

| 文件/模块 | 问题 | 影响 |
|----------|------|------|
| `frontend/src/App.tsx` (469 行) | 状态管理未分离 | 新功能开发效率下降 |
| `backend/app/persistence.py` | 仅支持 SQLite | 无法水平扩展 |
| `backend/app/tools/search.py` | 单一搜索源 Tavily | 单点故障风险 |
| `frontend/src/lib/strings.ts` | 无 i18n 框架 | 国际化无法扩展 |
| 全局 `APP_API_KEY` | 单一密钥无用户隔离 | 安全和多租户问题 |
